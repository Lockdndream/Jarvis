// Milestone 9B.1 — production foundation. Versions match spikes/android-presence,
// the only combination independently confirmed to build and run on the real
// target device (Galaxy S20 FE, Android 13/API 33) during Milestone 9B.0.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
